# Negative Tests

All `60` fail-closed cases passed. The suite used disposable files and sockets for actual hash, inode, symlink, bind, replacement-race, marker, and ownership checks and used bounded validators for the remaining policy states. It did not corrupt Phase 0A assets or the canonical repaired image. Full case records are in `NEGATIVE-TESTS.json`.
